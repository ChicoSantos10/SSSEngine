add_executable(SSSEngine WIN32 Win32.cpp)
target_link_options(SSSEngine PRIVATE -municode)
target_link_libraries(SSSEngine PRIVATE Xinput9_1_0.lib libksuser.a)
find_file(XAUDIO2_LIB XAudio2_9.dll)
if (XAUDIO2_LIB)
    message(STATUS "Found XAudio2: ${XAUDIO2_LIB}")
    target_link_libraries(SSSEngine PRIVATE ${XAUDIO2_LIB})
else ()
    message(FATAL_ERROR "Could not find XAudio2")
endif ()
#set_target_properties(XAudio2 PROPERTIES LINKER_LANGUAGE CXX)